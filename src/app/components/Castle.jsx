"use client";

import { useState, useEffect } from "react";
import { useAuth } from "../context/AuthContext";

export const Castle = () => {
const { user } = useAuth();

return (
    <div className="">
       
    </div>
);
}